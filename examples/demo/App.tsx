// Fixture for i18n-swarm's own PR-comment e2e check: two sole-child, un-keyed UI
// strings a fresh `check` run on this PR should flag.
export function App() {
  return (
    <div>
      <button>Save</button>
      <p>Start free trial</p>
    </div>
  );
}
